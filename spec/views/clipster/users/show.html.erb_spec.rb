require 'spec_helper'
require 'application_helper'
module Clipster
  describe "clipster/users/show.html.erb" do
    pending "pending"
    # before(:each) { @routes = Clipster::Engine.routes }

    # it "should respond with success" do

    #   @user = mock_model("User", {:id => 1, :email => "me@there.com"})
    #   @clips = Clip.public.where(:user_id => 1).page(1)

    #   render
    # end
  end
end