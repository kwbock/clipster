require 'spec_helper'

describe "clipster/clips/create.html.erb" do
  pending "add some examples to (or delete) #{__FILE__}"
  # before(:each) { @routes = Clipster::Engine.routes }

  # it "renders the create clip form" do
  #   assign(
  #       :clip,
  #       mock_model(Clipster::Clip)
  #     )
  #   p Clipster::Clip.lifespans

  #   assign(:languages, CodeRay::Scanners.all_plugins)

  #   render

  #   rendered.should contain("title")
  # end
end